apiVersion: machineconfiguration.openshift.io/v1
kind: MachineConfigPool
metadata:
  labels:
    # the label is used to identify the machine config pool for the infra-logmon role for kubeletconfig
    pools.operator.machineconfiguration.openshift.io/infra-observe: ""
  name: infra-observe
spec:
  machineConfigSelector:
    matchExpressions:
      - {key: machineconfiguration.openshift.io/role, operator: In, values: [worker,infra-observe]}
  maxUnavailable: null
  nodeSelector:
    matchLabels:
      node-role.kubernetes.io/infra-observe: ""
  paused: false