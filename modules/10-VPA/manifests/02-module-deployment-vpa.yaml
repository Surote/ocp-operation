apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: vpa-02-module
  namespace: 02-module
spec:
  resourcePolicy:
    containerPolicies:
    - containerName: '*'
      controlledResources:
      - cpu
      - memory
      maxAllowed:
        cpu: 1
        memory: 500Mi
      minAllowed:
        cpu: 10m
        memory: 25Mi
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: timechecker-deployment
  updatePolicy:
    updateMode: Off # Off means VPA will not update the resources of the pods, Auto means VPA will update the resources of the pods based on the usage of the pods.